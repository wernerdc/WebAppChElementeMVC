@model WebAppChElementeMVC.Models.Periode

@{
    ViewData["Title"] = "Details";
}

<div class="mt-2 mx-n3 px-3">
  <h4>Periode Details</h4>
</div>

<div class="pt-2">
  <h6>Periode</h6>
  <hr class="my-2">
  <dl class="row">
    <dt class="col-sm-2">
      @Html.DisplayNameFor(model => model.Name)
    </dt>
    <dd class="col-sm-10 mb-0">
      @Html.DisplayFor(model => model.Name)
    </dd>
    <dt class="col-sm-2">
      @Html.DisplayNameFor(model => model.Nummer)
    </dt>
    <dd class="col-sm-10 mb-0">
      @Html.DisplayFor(model => model.Nummer)
    </dd>
  </dl>
  
  <div>
    <br /> <br />
    <h6>Elemente</h6>
    <hr class="my-2">
    <dl class="row">
      <dd>

        <table class="table align-items-center mb-2">
          <thead>
            <tr>
              <th class="ps-2 text-secondary text-m font-weight-bolder opacity-7">Gruppe</th>
              <th class="ps-2 text-secondary text-m font-weight-bolder opacity-7">Ordnungszahl</th>
              <th class="ps-2 text-secondary text-m font-weight-bolder opacity-7">Symbol</th>
              <th class="ps-2 text-secondary text-m font-weight-bolder opacity-7">Element</th>
              <th class="ps-2 text-secondary text-m font-weight-bolder opacity-7">Zustand</th>
            </tr>
          </thead>
          <tbody>
            @foreach (var item in Model.Elemente)
            {
              <tr>
                <td>
                  @Html.DisplayFor(modelItem => item.Gruppe.Nummer)
                </td>
                <td>
                  @Html.DisplayFor(modelItem => item.Ordnungszahl)
                </td>
                <td>
                  @Html.DisplayFor(modelItem => item.Symbol)
                </td>
                <td>
                  @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                  @Html.DisplayFor(modelItem => item.Zustand.Bezeichnung)
                </td>
              </tr>
            }

            @* @foreach (var item in Model)
            {
              <tr>
                <td>
                  <h6 class="mb-0">
                    @Html.DisplayFor(modelItem => item.Name)
                  </h6>
                </td>
                <td>
                  <p class="mb-0">
                    @Html.DisplayFor(modelItem => item.Nummer)
                  </p>
                </td>
                <td class="align-middle text-end pe-0">
                  <div class="d-flex">
                    <a class="btn btn-primary w-auto btn-sm ms-auto me-2 mb-0" asp-action="Details" asp-route-id="@item.Id">Details</a>
                    <a class="btn btn-secondary w-auto btn-sm me-2 mb-0" asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                    <a class="btn btn-secondary w-auto btn-sm me-0 mb-0" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                  </div>
                </td>
              </tr>
            } *@
          </tbody>
        </table>



        @* <table class="table">
          <tr>
            <th>Gruppe</th>
            <th>Ordnungszahl</th>
            <th>Symbol</th>
            <th>Element</th>
            <th>Zustand</th>
          </tr>
          @foreach (var item in Model.Elemente)
          {
            <tr>
              <td>
                @Html.DisplayFor(modelItem => item.Gruppe.Nummer)
              </td>
              <td>
                @Html.DisplayFor(modelItem => item.Ordnungszahl)
              </td>
              <td>
                @Html.DisplayFor(modelItem => item.Symbol)
              </td>
              <td>
                @Html.DisplayFor(modelItem => item.Name)
              </td>
              <td>
                @Html.DisplayFor(modelItem => item.Zustand.Bezeichnung)
              </td>
            </tr>
          }
        </table> *@
      </dd>
    </dl>
  </div>
  <div class="d-flex mb-3">
    <a class="btn btn-primary w-auto me-3 mb-0" asp-action="Edit" asp-route-id="@Model?.Id">Edit Periode</a>
    <a class="btn btn-secondary w-auto me-3 mb-0" asp-action="Index">Back to List</a>
  </div>
  @* <div>
    <a asp-action="Edit" asp-route-id="@Model?.Id">Edit Periode</a>
    <a asp-action="Index">Back to List</a>
  </div>
  <form asp-action="Delete">
    <div class="form-group d-flex">
      <input type="hidden" asp-for="Id" />
      <input type="submit" value="Delete" class="btn btn-danger me-3" />
      <a asp-action="Index" class="btn btn-secondary me-2">Back to List</a>
    </div>
  </form> *@
</div>


@* <h1>Details</h1> *@

@* <div>
    <h4>Periode</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Nummer)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Nummer)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div> *@
